import { View, Text } from 'react-native';
import React from 'react';

export default function CartProduct(props) {
    const {
        cart,
        productDetail
    }=props
  return (
    <View>
      <Text></Text>
    </View>
  );
}
